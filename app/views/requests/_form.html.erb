<%= form_for(@request) do |f| %>
  <% if @request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request.errors.count, "error") %> prohibited this request from being saved:</h2>

      <ul>
      <% @request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <!--<div class="field">-->
    <!--<%= f.label :interaction_id %>-->
    <!--<%= select_tag(:interaction_id, options_from_collection_for_select(Interaction.all, :id, :interaction_title), :class => "form-control") %>-->

    <!--<%= f.label :requestee_id %>-->
    <!--<select class="selectpicker" multiple>-->
    <!--  <% User.all.each do |user| %>-->
    <!--    <option><%=user.first_name%> <%=user.last_name%></option>-->
    <!--  <% end %>-->
    <!--</select>-->




    <!--Need to add hash so that creates many requests at once-->
    <!--<%= select_tag(:requesstee_id, options_from_collection_for_select(Interaction.all, :id, :interaction_title), :class => "form-control") %>-->


  <!--</div>-->
  <div class="field">
    <%= f.hidden_field :requestor_id, :value => current_user.id %><br>
    <!--<%= hidden_field_tag current_user.id %>-->
  </div>
  <div class="field form-group">
    <%= f.label :requestee_id %><br>
    <!--<%= f.number_field :requestee_id %>-->
    <%= f.collection_select(:requestee_id, User.where.not(:id => current_user.id), :id, :last_name, { :class => "form-control" }) %>
  </div>
  <!--http://api.rubyonrails.org/classes/ActionView/Helpers/FormOptionsHelper.html#method-i-collection_select   LOOK AT THIS FOR MULTIPLE SELECT-->
  <div class="field">
    <%= f.label :interaction_id %><br>
    <%= f.collection_select(:interaction_id, current_user.interactions, :id, :interaction_title, {:selected => current_user.interactions.last.id, :class => "form-control" }) %>
  </div>
  <div class="field">
  <!-- WILL NEED TO UPDATE WITH PROPER GOAL -->
    <%= f.hidden_field :goal_id, :value => current_user.goal_attempts.first.goal.id %><br>
<!--     <%= f.number_field :goal_id %> -->
  </div>
  <div class="field">
    <%= f.label :feedback_note %><br>
    <%= f.text_area :feedback_note, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.hidden_field :feedback_completed, :value => 0 %><br>
    <!--<%= f.label :feedback_completed %><br>-->
    <!--<%= f.check_box :feedback_completed %>-->
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>

<% #TODO Fix goal entry %>
